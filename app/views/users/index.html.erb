<h3>Users</h3>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user_path(user) %>&nbsp;&nbsp;&nbsp;&nbsp;
      <%= link_to "Delete User", user_path(user), method: :delete %>
    </li>
  <% end %>
</ul>
